import React from "react";
import NetworkDebugger from "@/features/NetworkDebug/NetworkDebug";

const NetworkDebugPage: React.FC = () => {
  return <NetworkDebugger />;
};

export default NetworkDebugPage;
